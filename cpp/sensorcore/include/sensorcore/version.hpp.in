#pragma once

namespace sensorcore {

/// Full version string injected by CMake from scripts/version.sh (git tag).
constexpr const char* kVersion = "@SENSORCORE_VERSION@";

}  // namespace sensorcore
